{% extends 'base.html.twig' %}

{% block title %}Note
{% endblock %}

{% block body %}
	<h1>Note</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ note.id }}</td>
			</tr>
			<tr>
				<th>Title</th>
				<td>{{ note.title }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ note.description }}</td>
			</tr>

		</tbody>
	</table>

	<a href="{{ path('list') }}">back to list</a>

	<a href="{{ path('noteliste_edit', {'id': note.id}) }}">edit</a>

	{{ include('noteliste/_delete_form.html.twig') }}
{% endblock %}
